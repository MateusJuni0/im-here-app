{
  "name": "portugal-elite-ecosystem",
  "private": true,
  "workspaces": [
    "app",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=app",
    "install-all": "npm install --legacy-peer-deps"
  }
}
